#include "CAplication.hpp"

/**
 * @author Jakub Mikes
 * ASCIIart editor
 * Provides uncompressed BMP and TGA loading, editing and even creating animations
 * This project was created during Programming and algorithms 2 at FIT CTU
 */

int main ( ) {
    try {
        CAplication c;
    }
    catch ( std::exception & ex ) {
        std::cout << ex.what() << std::endl;
    }
    return 0;
}
